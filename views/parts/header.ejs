<!-- Header -->
<header id="header">
    <div class="inner">
      <%if (!user) { %>
      <a href="/home" class="logo"><strong>VSCODE</strong> online</a>
      <% }else{ %>
      <div class="logo">
        <strong>VSCODE</strong> <%= user.emails[0].value %>
      </div>
      <% } %>
      <nav id="nav">
        <a href="/home">Inicio</a>
        <a href="/about">Sobre</a>
        <%if (user) { %>
        <a href="/">IDE</a>
        <a href="/logout"> Sair</a>
        <% }else{ %>
        <a href="/auth/google">Login</a>
        <% } %>
      </nav>
      <a href="#navPanel" class="navPanelToggle"
        ><span class="fa fa-bars"></span
      ></a>
    </div>
  </header>